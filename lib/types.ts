export type LinkType = {
  id: number;
  label: string;
  link: string;
};

export interface ExperienceType {
  id: number;
  year: string;
  company: string;
  role: string;
  description: string;
}
